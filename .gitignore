# Scratch plans/specs/research — never committed (see operating model)
docs/superpowers/
# Superpowers agent scratch — plans, lane reports and reviews. Working notes, never
# a deliverable. Accidentally committed once in 099ba59 by a git add -A; published
# history is not rewritten, so this line is what stops it recurring.
.superpowers/

# Go
/graph2otel
/bin/
/dist/
*.test
*.out
coverage.*
/.tools/

# Third-party notices are a release artifact (regenerated per dependency bump,
# baked into the image / attached to the GitHub Release) — never committed.
/THIRD_PARTY_NOTICES.md

# Local config / secrets — NEVER commit
config.local.yaml
config.smoke.yaml
.env
.env.local
.secrets/

# Local checkpoint / state files from running the binary locally
/checkpoints/
*.checkpoint.json

.DS_Store

# Zensical docs-site build output (once the docs site exists)
site/

# Python bytecode from scripts/
__pycache__/
*.pyc

# Agent scratch worktrees (transient checkouts of this repo, never content)
.claude/worktrees/

# Injected by the m7kni.io hub at build time — never commit these.
# The hub copies its own SEO template, brand stylesheet and fonts into a clone
# and generates zensical.toml from docs.toml. A tracked copy here is drift, and
# is exactly what the manifest model exists to prevent.
/zensical.toml
docs/overrides/
docs/stylesheets/brand.css
docs/fonts/
