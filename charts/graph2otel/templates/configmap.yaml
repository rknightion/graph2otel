apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "graph2otel.fullname" . }}
  labels:
    {{- include "graph2otel.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- include "graph2otel.config" . | nindent 4 }}
