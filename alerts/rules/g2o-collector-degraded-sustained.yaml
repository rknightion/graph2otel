# GENERATED by grafana/build_rules.py — do not edit by hand. Edit the RULES or
# DETECTIONS list there, then run `make rules`.
#
# Grafana App Platform AlertRule (rules.alerting.grafana.app/v0alpha1). Deploy
# with `make rules-push` (see docs/deploying-observability.md), NOT with a bare
# `gcx resources push`: `grafana.app/folder` below is a substitution token, and
# an unresolved one fails with 403 rather than filing the rule somewhere wrong.
apiVersion: "rules.alerting.grafana.app/v0alpha1"
kind: "AlertRule"
metadata:
  name: "g2o-collector-degraded-sustained"
  annotations:
    grafana.app/folder: "REPLACE_WITH_FOLDER_UID"
    grafana.com/provenance: "api"
  labels:
    grafana.com/group: "graph2otel-alerts"
    grafana.com/group-index: "7"
spec:
  title: "graph2otel collector degraded for 6h"
  paused: false
  for: "30m0s"
  noDataState: "Ok"
  execErrState: "Error"
  trigger:
    interval: "5m"
  labels:
    severity: "warning"
    category: "self-observability"
    source: "graph2otel"
    pipeline: "graph2otel"
  annotations:
    summary: "Collector {{ $labels.collector }} has not had a single successful scrape in 6h (tenant {{ $labels.tenant_id }})"
    description: "graph2otel_scrape_success_ratio for collector={{ $labels.collector }}, tenant {{ $labels.tenant_id }} has been 0 for every sample in a 6h window. The collector is running and reporting — this is not staleness — but every run is coming back degraded or failed. The two causes worth separating are a REVOKED Graph consent grant (actionable: re-consent the app role) and an endpoint the tenant is simply not licensed for (not actionable: the collector correctly declines it forever, which is why g2o-collector-staleness deliberately stays silent for that case since #408). Read cause= on the WARN 'collector completed with degraded outcome' line, or graph2otel_scrape_outcomes_total, to tell them apart. Warning rather than critical because neither cause is a 3am page: the unlicensed one can never be actioned at all, and a revoked grant has already been broken for six hours by the time this fires. 6h with a 30m pending window tolerates the slowest collectors (24h intervals) restarting mid-window without a false positive."
    runbook_url: "https://m7kni.io/graph2otel/runbooks/#g2o-collector-degraded-sustained"
    __dashboardUid__: "graph2otel"
    __panelId__: "434"
    dashboard_path: "/d/graph2otel?dtab=Self-obs&viewPanel=434"
  expressions:
    A:
      model:
        datasource:
          type: "prometheus"
          uid: "grafanacloud-prom"
        editorMode: "code"
        expr: "max by (tenant_id, collector) (max_over_time(graph2otel_scrape_success_ratio[6h]))"
        instant: false
        range: true
        intervalMs: 1000
        maxDataPoints: 43200
        refId: "A"
      datasourceUID: "grafanacloud-prom"
      relativeTimeRange:
        from: "1h0m0s"
        to: "0s"
    B:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "A"
        reducer: "last"
        type: "reduce"
        refId: "B"
        intervalMs: 1000
        maxDataPoints: 43200
    C:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "B"
        type: "threshold"
        conditions:
          - type: "query"
            evaluator:
              type: "lt"
              params:
                - 1
            operator:
              type: "and"
            query:
              params:
                - "C"
            reducer:
              type: "last"
              params: []
        refId: "C"
        intervalMs: 1000
        maxDataPoints: 43200
      source: true
