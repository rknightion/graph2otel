# GENERATED by grafana/build_rules.py — do not edit by hand. Edit the RULES or
# DETECTIONS list there, then run `make rules`.
#
# Grafana App Platform AlertRule (rules.alerting.grafana.app/v0alpha1). Deploy
# with `make rules-push` (see docs/deploying-observability.md), NOT with a bare
# `gcx resources push`: `grafana.app/folder` below is a substitution token, and
# an unresolved one fails with 403 rather than filing the rule somewhere wrong.
apiVersion: "rules.alerting.grafana.app/v0alpha1"
kind: "AlertRule"
metadata:
  name: "g2o-collector-staleness"
  annotations:
    grafana.app/folder: "REPLACE_WITH_FOLDER_UID"
    grafana.com/provenance: "api"
  labels:
    grafana.com/group: "graph2otel-alerts"
    grafana.com/group-index: "6"
spec:
  title: "graph2otel collector scrape stale"
  paused: false
  for: "5m0s"
  noDataState: "Alerting"
  execErrState: "Error"
  trigger:
    interval: "5m"
  labels:
    severity: "critical"
    category: "self-observability"
    source: "graph2otel"
    pipeline: "graph2otel"
  annotations:
    summary: "Collector {{ $labels.collector }} is more than 3x its expected poll interval overdue (tenant {{ $labels.tenant_id }})"
    description: "graph2otel_scrape_staleness_seconds / graph2otel_collector_expected_interval_seconds for collector={{ $labels.collector }}, tenant {{ $labels.tenant_id }} has exceeded 3 for 5m — i.e. it has been more than 3x that COLLECTOR'S OWN effective poll interval (not a fixed second count) since its last successful scrape. Both metrics carry exactly (tenant_id, collector), so the division is a one-to-one vector match with no on()/ignoring() needed. 3x tolerates one missed poll plus backoff jitter without paging — several workloads have mandatory client-side rate limiters (reporting 5/10s, Identity Protection 1/s, Intune export 48/min) that make an occasional missed poll routine rather than a fault; a tighter 2x was rejected for exactly that reason. The rule sets its no-data state to Alerting because the whole query returning zero rows means every collector's self-obs signal went dark at once — the exporter process died, or a tenant's only collector was removed. It does NOT mean one collector's series disappearing: Grafana evaluates this rule per (tenant_id, collector) combination the query actually returns, so when ONE collector among several is deliberately removed (or disabled), its ratio series simply stops existing and its alert instance resolves silently — the other collectors' instances are unaffected and noDataState never applies to it. That silent, clean disappearance is the deliberately removed collector's correct outcome, not an accident."
    runbook_url: "https://m7kni.io/graph2otel/runbooks/#g2o-collector-staleness"
    __dashboardUid__: "graph2otel"
    __panelId__: "398"
    dashboard_path: "/d/graph2otel?dtab=Self-obs&viewPanel=398"
  expressions:
    A:
      model:
        datasource:
          type: "prometheus"
          uid: "grafanacloud-prom"
        editorMode: "code"
        expr: "max by (tenant_id, collector) (graph2otel_scrape_staleness_seconds) / max by (tenant_id, collector) (graph2otel_collector_expected_interval_seconds)"
        instant: false
        range: true
        intervalMs: 1000
        maxDataPoints: 43200
        refId: "A"
      datasourceUID: "grafanacloud-prom"
      relativeTimeRange:
        from: "1h0m0s"
        to: "0s"
    B:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "A"
        reducer: "last"
        type: "reduce"
        refId: "B"
        intervalMs: 1000
        maxDataPoints: 43200
    C:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "B"
        type: "threshold"
        conditions:
          - type: "query"
            evaluator:
              type: "gt"
              params:
                - 3
            operator:
              type: "and"
            query:
              params:
                - "C"
            reducer:
              type: "last"
              params: []
        refId: "C"
        intervalMs: 1000
        maxDataPoints: 43200
      source: true
