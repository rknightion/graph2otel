# GENERATED by grafana/build_rules.py — do not edit by hand. Edit the RULES or
# DETECTIONS list there, then run `just rules`.
#
# Grafana App Platform AlertRule (rules.alerting.grafana.app/v0alpha1). Deploy
# with `just rules-push` (see docs/deploying-observability.md), NOT with a bare
# `gcx resources push`: `grafana.app/folder` below is a substitution token, and
# an unresolved one fails with 403 rather than filing the rule somewhere wrong.
apiVersion: "rules.alerting.grafana.app/v0alpha1"
kind: "AlertRule"
metadata:
  name: "g2o-intune-cert-expiry-critical"
  annotations:
    grafana.app/folder: "REPLACE_WITH_FOLDER_UID"
    grafana.com/provenance: "api"
  labels:
    grafana.com/group: "graph2otel-alerts"
    grafana.com/group-index: "3"
spec:
  title: "Intune certificate expiring within 7 days"
  paused: true
  for: "15m0s"
  noDataState: "Ok"
  execErrState: "Error"
  trigger:
    interval: "5m"
  labels:
    severity: "critical"
    category: "credential-expiry"
    source: "intune"
    pipeline: "graph2otel"
    component: "certificate"
  annotations:
    summary: "Intune certificate profile \"{{ $labels.cert_profile_name }}\" has certificates expiring within 7 days (tenant {{ $labels.tenant_id }})"
    description: "intune_certificate_days_until_expiry is non-zero in the 0d_7d/expired buckets for cert_profile_name={{ $labels.cert_profile_name }}, tenant {{ $labels.tenant_id }}, for 15m. NOTE: cert expiry buckets are named differently from the entra credential buckets (0d_7d/7d_30d/30d_90d/over_90d/unknown vs lt_7d/lt_30d/lt_90d/gt_90d/expired); the runbook lists both ladders. Paused by default — enable once the certificates collector (beta) is on."
    runbook_url: "https://m7kni.io/graph2otel/runbooks/#g2o-intune-cert-expiry-critical"
    __dashboardUid__: "graph2otel"
    __panelId__: "239"
    dashboard_path: "/d/graph2otel?dtab=Intune&viewPanel=239"
  expressions:
    A:
      model:
        datasource:
          type: "prometheus"
          uid: "grafanacloud-prom"
        editorMode: "code"
        expr: "sum by (tenant_id, cert_profile_name) (intune_certificate_days_until_expiry{expiry_bucket=~\"0d_7d|expired\"})"
        instant: false
        range: true
        intervalMs: 1000
        maxDataPoints: 43200
        refId: "A"
      datasourceUID: "grafanacloud-prom"
      relativeTimeRange:
        from: "1h0m0s"
        to: "0s"
    B:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "A"
        reducer: "last"
        type: "reduce"
        refId: "B"
        intervalMs: 1000
        maxDataPoints: 43200
    C:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "B"
        type: "threshold"
        conditions:
          - type: "query"
            evaluator:
              type: "gt"
              params:
                - 0
            operator:
              type: "and"
            query:
              params:
                - "C"
            reducer:
              type: "last"
              params: []
        refId: "C"
        intervalMs: 1000
        maxDataPoints: 43200
      source: true
