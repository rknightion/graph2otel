# GENERATED by grafana/build_rules.py — do not edit by hand. Edit the RULES or
# DETECTIONS list there, then run `make rules`.
#
# Grafana App Platform AlertRule (rules.alerting.grafana.app/v0alpha1). Deploy
# with `make rules-push` (see docs/deploying-observability.md), NOT with a bare
# `gcx resources push`: `grafana.app/folder` below is a substitution token, and
# an unresolved one fails with 403 rather than filing the rule somewhere wrong.
apiVersion: "rules.alerting.grafana.app/v0alpha1"
kind: "AlertRule"
metadata:
  name: "g2o-record-over-horizon"
  annotations:
    grafana.app/folder: "REPLACE_WITH_FOLDER_UID"
    grafana.com/provenance: "api"
  labels:
    grafana.com/group: "graph2otel-alerts"
    grafana.com/group-index: "17"
spec:
  title: "graph2otel dropped records older than the backend accept window"
  paused: true
  noDataState: "Ok"
  execErrState: "Error"
  trigger:
    interval: "5m"
  labels:
    severity: "warning"
    category: "record-integrity"
    source: "graph2otel"
    pipeline: "graph2otel"
  annotations:
    summary: "Collector {{ $labels.collector }} dropped over-age records on {{ $labels.ingest_transport }} (tenant {{ $labels.tenant_id }})"
    description: "Records were dropped because their event time was older than the backend's 7-day accept window, so sending them would have been rejected per-entry and lost anyway (#401). This is real record loss. PAUSED by default on purpose: a nonzero value is EXPECTED on a blob-derived stream, which replays historical records and can age one past 7 days by ordinary aging with nothing misconfigured (#297 measured graph2otel's blob-ingested Intune stream at 6.95 days at the oldest). Enabling it as-is would page on normal behaviour. Unblock condition: measure your own steady-state rate on the 'Backend accept window' panel, raise the threshold above it, then enable."
    runbook_url: "https://m7kni.io/graph2otel/runbooks/#g2o-record-over-horizon"
    __dashboardUid__: "graph2otel"
    __panelId__: "483"
    dashboard_path: "/d/graph2otel?dtab=Self-obs&viewPanel=483"
  expressions:
    A:
      model:
        datasource:
          type: "prometheus"
          uid: "grafanacloud-prom"
        editorMode: "code"
        expr: "sum by (tenant_id, collector, ingest_transport) (increase(graph2otel_event_over_horizon_total[15m]))"
        instant: false
        range: true
        intervalMs: 1000
        maxDataPoints: 43200
        refId: "A"
      datasourceUID: "grafanacloud-prom"
      relativeTimeRange:
        from: "1h0m0s"
        to: "0s"
    B:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "A"
        reducer: "last"
        type: "reduce"
        refId: "B"
        intervalMs: 1000
        maxDataPoints: 43200
    C:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "B"
        type: "threshold"
        conditions:
          - type: "query"
            evaluator:
              type: "gt"
              params:
                - 0
            operator:
              type: "and"
            query:
              params:
                - "C"
            reducer:
              type: "last"
              params: []
        refId: "C"
        intervalMs: 1000
        maxDataPoints: 43200
      source: true
