# GENERATED by grafana/build_rules.py — do not edit by hand. Edit the RULES or
# DETECTIONS list there, then run `make rules`.
#
# Grafana App Platform AlertRule (rules.alerting.grafana.app/v0alpha1). Deploy
# with `make rules-push` (see docs/deploying-observability.md), NOT with a bare
# `gcx resources push`: `grafana.app/folder` below is a substitution token, and
# an unresolved one fails with 403 rather than filing the rule somewhere wrong.
apiVersion: "rules.alerting.grafana.app/v0alpha1"
kind: "AlertRule"
metadata:
  name: "g2o-record-integrity-loss"
  annotations:
    grafana.app/folder: "REPLACE_WITH_FOLDER_UID"
    grafana.com/provenance: "api"
  labels:
    grafana.com/group: "graph2otel-alerts"
    grafana.com/group-index: "8"
spec:
  title: "graph2otel dropped or errored source records"
  paused: false
  noDataState: "Ok"
  execErrState: "Error"
  trigger:
    interval: "5m"
  labels:
    severity: "warning"
    category: "record-integrity"
    source: "graph2otel"
    pipeline: "graph2otel"
  annotations:
    summary: "Collector {{ $labels.collector }} lost source records on {{ $labels.ingest_transport }} (tenant {{ $labels.tenant_id }})"
    description: "At least one source record was dropped or errored in the last 15m. `dropped` covers deliberate rejection such as an unparseable event timestamp; `errored` covers decode or processing failure. This is default-enabled because either outcome means fetched source data did not become useful telemetry."
    runbook_url: "https://m7kni.io/graph2otel/runbooks/#g2o-record-integrity-loss"
    __dashboardUid__: "graph2otel"
    __panelId__: "442"
    dashboard_path: "/d/graph2otel?dtab=Self-obs&viewPanel=442"
  expressions:
    A:
      model:
        datasource:
          type: "prometheus"
          uid: "grafanacloud-prom"
        editorMode: "code"
        expr: "sum by (tenant_id, collector, ingest_transport) (increase(graph2otel_record_outcomes_total{outcome=~\"dropped|errored\"}[15m]))"
        instant: false
        range: true
        intervalMs: 1000
        maxDataPoints: 43200
        refId: "A"
      datasourceUID: "grafanacloud-prom"
      relativeTimeRange:
        from: "1h0m0s"
        to: "0s"
    B:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "A"
        reducer: "last"
        type: "reduce"
        refId: "B"
        intervalMs: 1000
        maxDataPoints: 43200
    C:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "B"
        type: "threshold"
        conditions:
          - type: "query"
            evaluator:
              type: "gt"
              params:
                - 0
            operator:
              type: "and"
            query:
              params:
                - "C"
            reducer:
              type: "last"
              params: []
        refId: "C"
        intervalMs: 1000
        maxDataPoints: 43200
      source: true
