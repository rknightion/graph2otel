# GENERATED by grafana/build_rules.py — do not edit by hand. Edit the RULES or
# DETECTIONS list there, then run `make rules`.
#
# Grafana App Platform AlertRule (rules.alerting.grafana.app/v0alpha1). Deploy
# with `make rules-push` (see docs/deploying-observability.md), NOT with a bare
# `gcx resources push`: `grafana.app/folder` below is a substitution token, and
# an unresolved one fails with 403 rather than filing the rule somewhere wrong.
apiVersion: "rules.alerting.grafana.app/v0alpha1"
kind: "AlertRule"
metadata:
  name: "g2o-detect-security-incident-active"
  annotations:
    grafana.app/folder: "REPLACE_WITH_FOLDER_UID"
    grafana.com/provenance: "api"
  labels:
    grafana.com/group: "graph2otel-detections"
    grafana.com/group-index: "2"
spec:
  title: "Security incident, medium or high, active"
  paused: true
  noDataState: "Ok"
  execErrState: "Error"
  trigger:
    interval: "5m"
  labels:
    severity: "warning"
    source: "entra"
    category: "identity-threat"
    pipeline: "graph2otel"
  annotations:
    summary: "An active medium/high security incident is open"
    description: "Incidents are the CORRELATION layer above individual alerts: one incident groups related alerts Microsoft believes are the same attack. This therefore OVERLAPS with the unresolved-alert detection above, and a single security event will usually match both. That is deliberate, since the incident carries the correlation and the alert carries the detail, but decide which one you want to page on rather than enabling both and being paged twice for one event."
    tuning_required: "Enable this or the alert rule, not both, until you have watched a real incident arrive and seen which gives your responders the better entry point."
    runbook_url: "https://m7kni.io/graph2otel/runbooks/#g2o-detect-security-incident-active"
    __dashboardUid__: "graph2otel"
    __panelId__: "318"
    dashboard_path: "/d/graph2otel?dtab=Defender&viewPanel=318"
  expressions:
    A:
      model:
        refId: "A"
        datasource:
          type: "loki"
          uid: "grafanacloud-logs"
        expr: "sum (count_over_time({service_name=\"graph2otel\"} | event_name=`entra.security_incident` | severity=~`(?i)(high|medium)` | status=~`(?i)(active|inProgress)` [5m]))"
        queryType: "range"
        editorMode: "code"
        intervalMs: 1000
        maxDataPoints: 43200
      datasourceUID: "grafanacloud-logs"
      relativeTimeRange:
        from: "1h0m0s"
        to: "0s"
    B:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "A"
        reducer: "last"
        type: "reduce"
        refId: "B"
        intervalMs: 1000
        maxDataPoints: 43200
    C:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "B"
        type: "threshold"
        conditions:
          - type: "query"
            evaluator:
              type: "gt"
              params:
                - 0
            operator:
              type: "and"
            query:
              params:
                - "C"
            reducer:
              type: "last"
              params: []
        refId: "C"
        intervalMs: 1000
        maxDataPoints: 43200
      source: true
