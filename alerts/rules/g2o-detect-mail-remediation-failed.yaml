# GENERATED by grafana/build_rules.py — do not edit by hand. Edit the RULES or
# DETECTIONS list there, then run `make rules`.
#
# Grafana App Platform AlertRule (rules.alerting.grafana.app/v0alpha1). Deploy
# with `make rules-push` (see docs/deploying-observability.md), NOT with a bare
# `gcx resources push`: `grafana.app/folder` below is a substitution token, and
# an unresolved one fails with 403 rather than filing the rule somewhere wrong.
apiVersion: "rules.alerting.grafana.app/v0alpha1"
kind: "AlertRule"
metadata:
  name: "g2o-detect-mail-remediation-failed"
  annotations:
    grafana.app/folder: "REPLACE_WITH_FOLDER_UID"
    grafana.com/provenance: "api"
  labels:
    grafana.com/group: "graph2otel-detections"
    grafana.com/group-index: "10"
spec:
  title: "Post-delivery mail remediation did not succeed"
  paused: true
  noDataState: "Ok"
  execErrState: "Error"
  trigger:
    interval: "5m"
  labels:
    severity: "warning"
    source: "defender"
    category: "identity-threat"
    pipeline: "graph2otel"
  annotations:
    summary: "Defender tried to remove a delivered message and did not succeed"
    description: "Zero-hour auto purge and manual remediation exist because a message can be reclassified as malicious after it has already landed in a mailbox. When that removal FAILS the message is still sitting in an inbox that Microsoft has already decided is dangerous, and nothing else tells you so — the alert says the threat was found, the remediation record says whether it was actually removed. Read action_type, action_trigger and recipient_email_address, then remove the message by hand."
    tuning_required: "The filter is two terms because a negative label filter also matches a record that carries no action_result at all: LogQL treats a missing structured-metadata key as the empty string, so the presence term `action_result=~`.+`` is what keeps this from firing on every remediation record. Run the post-delivery hunt to see which result values your tenant emits and how often a non-success one is a transient retry rather than a real miss. Needs the Defender advanced-hunting blob ingest configured; without it the stream is absent, not clean."
    runbook_url: "https://m7kni.io/graph2otel/runbooks/#g2o-detect-mail-remediation-failed"
    __dashboardUid__: "graph2otel"
    __panelId__: "266"
    dashboard_path: "/d/graph2otel?dtab=Defender&viewPanel=266"
  expressions:
    A:
      model:
        refId: "A"
        datasource:
          type: "loki"
          uid: "grafanacloud-logs"
        expr: "sum (count_over_time({service_name=\"graph2otel\"} | event_name=`defender.email_post_delivery` | action_result=~`.+` | action_result!~`(?i)success` [5m]))"
        queryType: "range"
        editorMode: "code"
        intervalMs: 1000
        maxDataPoints: 43200
      datasourceUID: "grafanacloud-logs"
      relativeTimeRange:
        from: "1h0m0s"
        to: "0s"
    B:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "A"
        reducer: "last"
        type: "reduce"
        refId: "B"
        intervalMs: 1000
        maxDataPoints: 43200
    C:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "B"
        type: "threshold"
        conditions:
          - type: "query"
            evaluator:
              type: "gt"
              params:
                - 0
            operator:
              type: "and"
            query:
              params:
                - "C"
            reducer:
              type: "last"
              params: []
        refId: "C"
        intervalMs: 1000
        maxDataPoints: 43200
      source: true
