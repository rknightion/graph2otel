# GENERATED by grafana/build_rules.py — do not edit by hand. Edit the RULES or
# DETECTIONS list there, then run `make rules`.
#
# Grafana App Platform AlertRule (rules.alerting.grafana.app/v0alpha1). Deploy
# with `make rules-push` (see docs/deploying-observability.md), NOT with a bare
# `gcx resources push`: `grafana.app/folder` below is a substitution token, and
# an unresolved one fails with 403 rather than filing the rule somewhere wrong.
apiVersion: "rules.alerting.grafana.app/v0alpha1"
kind: "AlertRule"
metadata:
  name: "g2o-intune-apple-token-expiry-critical"
  annotations:
    grafana.app/folder: "REPLACE_WITH_FOLDER_UID"
    grafana.com/provenance: "api"
  labels:
    grafana.com/group: "graph2otel-alerts"
    grafana.com/group-index: "2"
spec:
  title: "Intune Apple MDM token expiring soon"
  paused: true
  for: "15m0s"
  noDataState: "Ok"
  execErrState: "Error"
  trigger:
    interval: "5m"
  labels:
    severity: "critical"
    category: "credential-expiry"
    source: "intune"
    pipeline: "graph2otel"
    component: "apple-token"
  annotations:
    summary: "Apple {{ $labels.type }} token \"{{ $labels.token_name }}\" expires in under 14 days (tenant {{ $labels.tenant_id }})"
    description: "intune_apple_token_days_until_expiry (APNS/VPP/DEP) is below 14 for token_name={{ $labels.token_name }}, tenant {{ $labels.tenant_id }}, for 15m. Unlike the bucketed metrics, this is a raw days-remaining gauge over a tiny admin-configured token set, so the threshold is an exact day count — tune freely. Paused by default (companion of the primary compliance/credential set) — enable once the apple_tokens collector is on for tenants that use Apple MDM."
    runbook_url: "https://m7kni.io/graph2otel/runbooks/#g2o-intune-apple-token-expiry-critical"
    __dashboardUid__: "graph2otel"
    __panelId__: "211"
    dashboard_path: "/d/graph2otel?dtab=Intune&viewPanel=211"
  expressions:
    A:
      model:
        datasource:
          type: "prometheus"
          uid: "grafanacloud-prom"
        editorMode: "code"
        expr: "min by (tenant_id, type, token_name) (intune_apple_token_days_until_expiry)"
        instant: false
        range: true
        intervalMs: 1000
        maxDataPoints: 43200
        refId: "A"
      datasourceUID: "grafanacloud-prom"
      relativeTimeRange:
        from: "1h0m0s"
        to: "0s"
    B:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "A"
        reducer: "last"
        type: "reduce"
        refId: "B"
        intervalMs: 1000
        maxDataPoints: 43200
    C:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "B"
        type: "threshold"
        conditions:
          - type: "query"
            evaluator:
              type: "lt"
              params:
                - 14
            operator:
              type: "and"
            query:
              params:
                - "C"
            reducer:
              type: "last"
              params: []
        refId: "C"
        intervalMs: 1000
        maxDataPoints: 43200
      source: true
