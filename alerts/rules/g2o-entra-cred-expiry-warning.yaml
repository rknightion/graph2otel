# GENERATED by grafana/build_rules.py — do not edit by hand. Edit the RULES or
# DETECTIONS list there, then run `make rules`.
#
# Grafana App Platform AlertRule (rules.alerting.grafana.app/v0alpha1). Deploy
# with `make rules-push` (see docs/deploying-observability.md), NOT with a bare
# `gcx resources push`: `grafana.app/folder` below is a substitution token, and
# an unresolved one fails with 403 rather than filing the rule somewhere wrong.
apiVersion: "rules.alerting.grafana.app/v0alpha1"
kind: "AlertRule"
metadata:
  name: "g2o-entra-cred-expiry-warning"
  annotations:
    grafana.app/folder: "REPLACE_WITH_FOLDER_UID"
    grafana.com/provenance: "api"
  labels:
    grafana.com/group: "graph2otel-alerts"
    grafana.com/group-index: "1"
spec:
  title: "Entra app/SP credential expiring within 30 days"
  paused: true
  for: "15m0s"
  noDataState: "Ok"
  execErrState: "Error"
  trigger:
    interval: "5m"
  labels:
    severity: "warning"
    category: "credential-expiry"
    source: "entra"
    pipeline: "graph2otel"
  annotations:
    summary: "{{ $labels.owner_type }} {{ $labels.credential_type }} credential(s) expiring within 30 days (tenant {{ $labels.tenant_id }})"
    description: "Companion to g2o-entra-cred-expiry-critical: earlier warning tier on the lt_30d bucket. Paused by default — enable once you've picked a rotation-lead-time that matches your tenant's actual credential renewal process."
    runbook_url: "https://m7kni.io/graph2otel/runbooks/#g2o-entra-cred-expiry-warning"
    __dashboardUid__: "graph2otel"
    __panelId__: "30"
    dashboard_path: "/d/graph2otel?dtab=Entra&viewPanel=30"
  expressions:
    A:
      model:
        datasource:
          type: "prometheus"
          uid: "grafanacloud-prom"
        editorMode: "code"
        expr: "sum by (tenant_id, owner_type, credential_type) (entra_credentials_expiring_total{expiry_bucket=\"lt_30d\"})"
        instant: false
        range: true
        intervalMs: 1000
        maxDataPoints: 43200
        refId: "A"
      datasourceUID: "grafanacloud-prom"
      relativeTimeRange:
        from: "1h0m0s"
        to: "0s"
    B:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "A"
        reducer: "last"
        type: "reduce"
        refId: "B"
        intervalMs: 1000
        maxDataPoints: 43200
    C:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "B"
        type: "threshold"
        conditions:
          - type: "query"
            evaluator:
              type: "gt"
              params:
                - 0
            operator:
              type: "and"
            query:
              params:
                - "C"
            reducer:
              type: "last"
              params: []
        refId: "C"
        intervalMs: 1000
        maxDataPoints: 43200
      source: true
