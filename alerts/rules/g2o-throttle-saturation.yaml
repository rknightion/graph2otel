# GENERATED by grafana/build_rules.py — do not edit by hand. Edit the RULES or
# DETECTIONS list there, then run `make rules`.
#
# Grafana App Platform AlertRule (rules.alerting.grafana.app/v0alpha1). Deploy
# with `make rules-push` (see docs/deploying-observability.md), NOT with a bare
# `gcx resources push`: `grafana.app/folder` below is a substitution token, and
# an unresolved one fails with 403 rather than filing the rule somewhere wrong.
apiVersion: "rules.alerting.grafana.app/v0alpha1"
kind: "AlertRule"
metadata:
  name: "g2o-throttle-saturation"
  annotations:
    grafana.app/folder: "REPLACE_WITH_FOLDER_UID"
    grafana.com/provenance: "api"
  labels:
    grafana.com/group: "graph2otel-alerts"
    grafana.com/group-index: "10"
spec:
  title: "graph2otel sustained Graph API throttling"
  paused: false
  for: "15m0s"
  noDataState: "Ok"
  execErrState: "Error"
  trigger:
    interval: "5m"
  labels:
    severity: "warning"
    category: "throttle"
    source: "graph2otel"
    pipeline: "graph2otel"
  annotations:
    summary: "Sustained 429s from Microsoft Graph on the {{ $labels.workload }} workload (tenant {{ $labels.tenant_id }})"
    description: "rate(graph2otel_throttle_count_total[10m]) (429 responses observed by the client-side rate limiter) is above 0 for workload={{ $labels.workload }}, tenant {{ $labels.tenant_id }}, sustained for 15m — i.e. throttling that isn't a one-off blip but is still happening 10-15 minutes later. The rule treats no data as OK: zero throttle events is the healthy steady state, not an absence worth alerting on. The runbook has the per-workload ceiling context (none of the reporting 5/10s, Identity Protection 1/s, Intune reports-export 48/min, or directory RU workloads send Retry-After, so silent throttling degrades data freshness before anything else visibly breaks)."
    runbook_url: "https://m7kni.io/graph2otel/runbooks/#g2o-throttle-saturation"
    __dashboardUid__: "graph2otel"
    __panelId__: "388"
    dashboard_path: "/d/graph2otel?dtab=Self-obs&viewPanel=388"
  expressions:
    A:
      model:
        datasource:
          type: "prometheus"
          uid: "grafanacloud-prom"
        editorMode: "code"
        expr: "sum by (tenant_id, workload) (rate(graph2otel_throttle_count_total[10m]))"
        instant: false
        range: true
        intervalMs: 1000
        maxDataPoints: 43200
        refId: "A"
      datasourceUID: "grafanacloud-prom"
      relativeTimeRange:
        from: "1h0m0s"
        to: "0s"
    B:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "A"
        reducer: "last"
        type: "reduce"
        refId: "B"
        intervalMs: 1000
        maxDataPoints: 43200
    C:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "B"
        type: "threshold"
        conditions:
          - type: "query"
            evaluator:
              type: "gt"
              params:
                - 0
            operator:
              type: "and"
            query:
              params:
                - "C"
            reducer:
              type: "last"
              params: []
        refId: "C"
        intervalMs: 1000
        maxDataPoints: 43200
      source: true
