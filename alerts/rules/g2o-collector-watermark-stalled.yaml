# GENERATED by grafana/build_rules.py — do not edit by hand. Edit the RULES or
# DETECTIONS list there, then run `make rules`.
#
# Grafana App Platform AlertRule (rules.alerting.grafana.app/v0alpha1). Deploy
# with `make rules-push` (see docs/deploying-observability.md), NOT with a bare
# `gcx resources push`: `grafana.app/folder` below is a substitution token, and
# an unresolved one fails with 403 rather than filing the rule somewhere wrong.
apiVersion: "rules.alerting.grafana.app/v0alpha1"
kind: "AlertRule"
metadata:
  name: "g2o-collector-watermark-stalled"
  annotations:
    grafana.app/folder: "REPLACE_WITH_FOLDER_UID"
    grafana.com/provenance: "api"
  labels:
    grafana.com/group: "graph2otel-alerts"
    grafana.com/group-index: "8"
spec:
  title: "graph2otel window watermark not advancing"
  paused: true
  for: "30m0s"
  noDataState: "Ok"
  execErrState: "Error"
  trigger:
    interval: "5m"
  labels:
    severity: "critical"
    category: "self-observability"
    source: "graph2otel"
    pipeline: "graph2otel"
  annotations:
    summary: "Collector {{ $labels.collector }} has a window watermark more than 20x its poll interval behind now (tenant {{ $labels.tenant_id }})"
    description: "The durable checkpoint watermark for collector={{ $labels.collector }}, tenant {{ $labels.tenant_id }} has stopped advancing: it is more than 20x that COLLECTOR'S OWN effective poll interval behind wall-clock. This is the #417 fingerprint — a livelocked window poller that re-fetches one frozen range every tick, dedupes it, and reports a perfectly healthy scrape. Expect graph2otel_scrape_success_ratio to be 1, availability to read healthy, and graph2otel_record_outcomes_total to show fetched == mapped == deduped with NO emitted series at all; none of that is evidence against this alert, it is the shape of the fault. PAUSED, and the threshold is a placeholder. The unblock condition is a MEASUREMENT that cannot be taken until this metric has shipped: observe (time() - graph2otel_collector_watermark_timestamp_seconds) / graph2otel_collector_expected_interval_seconds across every window collector for at least one full week on a live tenant, take the per-collector maximum over that week, and set the threshold above the largest of them. 20x is a guess chosen to clear the per-collector SafetyLag (which is subtracted from the watermark and is NOT exported, so it inflates this ratio by an unknown amount that hurts the fastest collectors most) — it is not a measured bound. Enabling it before that measurement risks firing on correct data, which is the specific failure #422 names: an alert that fires on correct data trains the reader to ignore the signal, and that is how the next 11-day freeze goes unnoticed."
    runbook_url: "https://m7kni.io/graph2otel/runbooks/#g2o-collector-watermark-stalled"
    __dashboardUid__: "graph2otel"
    __panelId__: "440"
    dashboard_path: "/d/graph2otel?dtab=Self-obs&viewPanel=440"
  expressions:
    A:
      model:
        datasource:
          type: "prometheus"
          uid: "grafanacloud-prom"
        editorMode: "code"
        expr: "(time() - max by (tenant_id, collector) (graph2otel_collector_watermark_timestamp_seconds)) / max by (tenant_id, collector) (graph2otel_collector_expected_interval_seconds)"
        instant: false
        range: true
        intervalMs: 1000
        maxDataPoints: 43200
        refId: "A"
      datasourceUID: "grafanacloud-prom"
      relativeTimeRange:
        from: "1h0m0s"
        to: "0s"
    B:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "A"
        reducer: "last"
        type: "reduce"
        refId: "B"
        intervalMs: 1000
        maxDataPoints: 43200
    C:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "B"
        type: "threshold"
        conditions:
          - type: "query"
            evaluator:
              type: "gt"
              params:
                - 20
            operator:
              type: "and"
            query:
              params:
                - "C"
            reducer:
              type: "last"
              params: []
        refId: "C"
        intervalMs: 1000
        maxDataPoints: 43200
      source: true
