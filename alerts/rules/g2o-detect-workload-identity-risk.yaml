# GENERATED by grafana/build_rules.py — do not edit by hand. Edit the RULES or
# DETECTIONS list there, then run `make rules`.
#
# Grafana App Platform AlertRule (rules.alerting.grafana.app/v0alpha1). Deploy
# with `make rules-push` (see docs/deploying-observability.md), NOT with a bare
# `gcx resources push`: `grafana.app/folder` below is a substitution token, and
# an unresolved one fails with 403 rather than filing the rule somewhere wrong.
apiVersion: "rules.alerting.grafana.app/v0alpha1"
kind: "AlertRule"
metadata:
  name: "g2o-detect-workload-identity-risk"
  annotations:
    grafana.app/folder: "REPLACE_WITH_FOLDER_UID"
    grafana.com/provenance: "api"
  labels:
    grafana.com/group: "graph2otel-detections"
    grafana.com/group-index: "8"
spec:
  title: "Workload identity flagged at risk or confirmed compromised"
  paused: true
  noDataState: "Ok"
  execErrState: "Error"
  trigger:
    interval: "5m"
  labels:
    severity: "critical"
    source: "entra"
    category: "identity-threat"
    pipeline: "graph2otel"
  annotations:
    summary: "A service principal was flagged at risk or confirmed compromised"
    description: "The workload-identity half of Identity Protection: a service principal whose credential leaked, or whose activity Microsoft scored anomalous. This matters more than the user equivalent and gets watched less. A service principal has no MFA to fall back on, its credential is often in a pipeline variable rather than a vault, and nothing prompts a human when it is used from somewhere new. Check service_principal_name, app_id and risk_detail, then rotate the credential rather than only dismissing the detection. This is the portable counterpart to pinning each automation identity to its own expected source, which cannot ship here because those values are tenant infrastructure."
    tuning_required: "The endpoint answers with real detections even on a tenant without Workload Identities Premium, live-measured, so absence of data here is genuinely absence of detections rather than a licence wall. It is usually silent, which makes the threshold of zero plausible and unmeasured at the same time: run the workload-identity risk hunt over 90 days, and if it returns nothing, keep the rule paused until you have confirmed the stream works by synthesising one detection."
    runbook_url: "https://m7kni.io/graph2otel/runbooks/#g2o-detect-workload-identity-risk"
    __dashboardUid__: "graph2otel"
    __panelId__: "92"
    dashboard_path: "/d/graph2otel?dtab=Entra&viewPanel=92"
  expressions:
    A:
      model:
        refId: "A"
        datasource:
          type: "loki"
          uid: "grafanacloud-logs"
        expr: "sum (count_over_time({service_name=\"graph2otel\"} | event_name=`entra.service_principal_risk_detection` | risk_level=~`(?i)(high|medium)` | risk_state=~`(?i)(atrisk|confirmedcompromised)` [5m]))"
        queryType: "range"
        editorMode: "code"
        intervalMs: 1000
        maxDataPoints: 43200
      datasourceUID: "grafanacloud-logs"
      relativeTimeRange:
        from: "1h0m0s"
        to: "0s"
    B:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "A"
        reducer: "last"
        type: "reduce"
        refId: "B"
        intervalMs: 1000
        maxDataPoints: 43200
    C:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "B"
        type: "threshold"
        conditions:
          - type: "query"
            evaluator:
              type: "gt"
              params:
                - 0
            operator:
              type: "and"
            query:
              params:
                - "C"
            reducer:
              type: "last"
              params: []
        refId: "C"
        intervalMs: 1000
        maxDataPoints: 43200
      source: true
