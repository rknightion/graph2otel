# GENERATED by grafana/build_rules.py — do not edit by hand. Edit the RULES or
# DETECTIONS list there, then run `make rules`.
#
# Grafana App Platform AlertRule (rules.alerting.grafana.app/v0alpha1). Deploy
# with `make rules-push` (see docs/deploying-observability.md), NOT with a bare
# `gcx resources push`: `grafana.app/folder` below is a substitution token, and
# an unresolved one fails with 403 rather than filing the rule somewhere wrong.
apiVersion: "rules.alerting.grafana.app/v0alpha1"
kind: "AlertRule"
metadata:
  name: "g2o-detect-mailbox-permission-grant"
  annotations:
    grafana.app/folder: "REPLACE_WITH_FOLDER_UID"
    grafana.com/provenance: "api"
  labels:
    grafana.com/group: "graph2otel-detections"
    grafana.com/group-index: "6"
spec:
  title: "Mailbox, recipient or folder permission granted"
  paused: true
  noDataState: "Ok"
  execErrState: "Error"
  trigger:
    interval: "5m"
  labels:
    severity: "warning"
    source: "m365"
    category: "identity-threat"
    pipeline: "graph2otel"
  annotations:
    summary: "A mailbox delegation or folder permission was granted"
    description: "Delegate access to a mailbox is durable, survives the owner's password reset, and is invisible to the owner, which is why granting it is a standard persistence and collection step once an attacker holds an administrative session. Microsoft tracks it as additional email delegate permissions, MITRE T1098.002. It shares the m365.audit operation field with the inbox-rule detection above and is a separate rule on purpose: an inbox rule is usually the mailbox owner's own doing and noisy, while a delegation grant is an administrative act, rarer, and remediated differently. Combining them would force one threshold onto two very different base rates."
    tuning_required: "Shared mailboxes, resource calendars and migration tooling all produce legitimate grants in bursts. Run the audit-operation hunt for 30 days to separate your routine grant sources from the rest, and to confirm the operation spelling, before enabling."
    runbook_url: "https://m7kni.io/graph2otel/runbooks/#g2o-detect-mailbox-permission-grant"
    __dashboardUid__: "graph2otel"
    __panelId__: "339"
    dashboard_path: "/d/graph2otel?dtab=M365&viewPanel=339"
  expressions:
    A:
      model:
        refId: "A"
        datasource:
          type: "loki"
          uid: "grafanacloud-logs"
        expr: "sum (count_over_time({service_name=\"graph2otel\"} | event_name=`m365.audit` | operation=~`(?i).*(mailboxpermission|recipientpermission|folderpermission).*` [5m]))"
        queryType: "range"
        editorMode: "code"
        intervalMs: 1000
        maxDataPoints: 43200
      datasourceUID: "grafanacloud-logs"
      relativeTimeRange:
        from: "1h0m0s"
        to: "0s"
    B:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "A"
        reducer: "last"
        type: "reduce"
        refId: "B"
        intervalMs: 1000
        maxDataPoints: 43200
    C:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "B"
        type: "threshold"
        conditions:
          - type: "query"
            evaluator:
              type: "gt"
              params:
                - 0
            operator:
              type: "and"
            query:
              params:
                - "C"
            reducer:
              type: "last"
              params: []
        refId: "C"
        intervalMs: 1000
        maxDataPoints: 43200
      source: true
