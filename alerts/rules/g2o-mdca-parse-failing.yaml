# GENERATED by grafana/build_rules.py — do not edit by hand. Edit the RULES or
# DETECTIONS list there, then run `make rules`.
#
# Grafana App Platform AlertRule (rules.alerting.grafana.app/v0alpha1). Deploy
# with `make rules-push` (see docs/deploying-observability.md), NOT with a bare
# `gcx resources push`: `grafana.app/folder` below is a substitution token, and
# an unresolved one fails with 403 rather than filing the rule somewhere wrong.
apiVersion: "rules.alerting.grafana.app/v0alpha1"
kind: "AlertRule"
metadata:
  name: "g2o-mdca-parse-failing"
  annotations:
    grafana.app/folder: "REPLACE_WITH_FOLDER_UID"
    grafana.com/provenance: "api"
  labels:
    grafana.com/group: "graph2otel-alerts"
    grafana.com/group-index: "13"
spec:
  title: "graph2otel MDCA Cloud Discovery parse failing"
  paused: false
  for: "5m0s"
  noDataState: "Ok"
  execErrState: "Error"
  trigger:
    interval: "5m"
  labels:
    severity: "warning"
    category: "mdca-discovery"
    source: "graph2otel"
    pipeline: "graph2otel"
  annotations:
    summary: "MDCA Cloud Discovery parse failing on stream {{ $labels.input_stream_id }} (tenant {{ $labels.tenant_id }})"
    description: "increase(mdca_discovery_parse_tasks_total{is_success=\"false\"}[1h]) > 0 for input_stream_id={{ $labels.input_stream_id }}, template={{ $labels.template }}, tenant {{ $labels.tenant_id }}: at least one Cloud Discovery parse task FAILED in the last hour while the upload almost certainly reported HTTP 200. The template label names which failure (e.g. REPOOPER_COMPLETION_STATUS_BASELOGPARSER_UNEXPECTED_FORMAT = malformed log format). This is the 22-silent-failures outage from #145. Default-enabled; pair with g2o-mdca-uploads-stopped, which catches a dead uploader this rule cannot see."
    runbook_url: "https://m7kni.io/graph2otel/runbooks/#g2o-mdca-parse-failing"
    __dashboardUid__: "graph2otel"
    __panelId__: "260"
    dashboard_path: "/d/graph2otel?dtab=Defender&viewPanel=260"
  expressions:
    A:
      model:
        datasource:
          type: "prometheus"
          uid: "grafanacloud-prom"
        editorMode: "code"
        expr: "sum by (tenant_id, input_stream_id, template) (increase(mdca_discovery_parse_tasks_total{is_success=\"false\"}[1h]))"
        instant: false
        range: true
        intervalMs: 1000
        maxDataPoints: 43200
        refId: "A"
      datasourceUID: "grafanacloud-prom"
      relativeTimeRange:
        from: "1h0m0s"
        to: "0s"
    B:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "A"
        reducer: "last"
        type: "reduce"
        refId: "B"
        intervalMs: 1000
        maxDataPoints: 43200
    C:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "B"
        type: "threshold"
        conditions:
          - type: "query"
            evaluator:
              type: "gt"
              params:
                - 0
            operator:
              type: "and"
            query:
              params:
                - "C"
            reducer:
              type: "last"
              params: []
        refId: "C"
        intervalMs: 1000
        maxDataPoints: 43200
      source: true
