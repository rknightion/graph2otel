# GENERATED by grafana/build_rules.py — do not edit by hand. Edit the RULES or
# DETECTIONS list there, then run `make rules`.
#
# Grafana App Platform AlertRule (rules.alerting.grafana.app/v0alpha1). Deploy
# with `make rules-push` (see docs/deploying-observability.md), NOT with a bare
# `gcx resources push`: `grafana.app/folder` below is a substitution token, and
# an unresolved one fails with 403 rather than filing the rule somewhere wrong.
apiVersion: "rules.alerting.grafana.app/v0alpha1"
kind: "AlertRule"
metadata:
  name: "g2o-intune-compliance-ratio-low"
  annotations:
    grafana.app/folder: "REPLACE_WITH_FOLDER_UID"
    grafana.com/provenance: "api"
  labels:
    grafana.com/group: "graph2otel-alerts"
    grafana.com/group-index: "4"
spec:
  title: "Intune compliant-device fraction below 90%"
  paused: false
  for: "30m0s"
  noDataState: "Ok"
  execErrState: "Error"
  trigger:
    interval: "5m"
  labels:
    severity: "warning"
    category: "compliance"
    source: "intune"
    pipeline: "graph2otel"
  annotations:
    summary: "Intune compliant-device fraction below 90% (tenant {{ $labels.tenant_id }})"
    description: "compliant / total intune_compliance_devices is below 0.9 for tenant {{ $labels.tenant_id }} for 30m. The `and >= 5` fleet-size guard suppresses the ratio on tiny fleets where one non-compliant device swings the percentage. The runbook covers the false-positive notes."
    runbook_url: "https://m7kni.io/graph2otel/runbooks/#g2o-intune-compliance-ratio-low"
    __dashboardUid__: "graph2otel"
    __panelId__: "128"
    dashboard_path: "/d/graph2otel?dtab=Intune&viewPanel=128"
  expressions:
    A:
      model:
        datasource:
          type: "prometheus"
          uid: "grafanacloud-prom"
        editorMode: "code"
        expr: "(sum by (tenant_id) (intune_compliance_devices{state=\"compliant\"}) / sum by (tenant_id) (intune_compliance_devices)) and sum by (tenant_id) (intune_compliance_devices) >= 5"
        instant: false
        range: true
        intervalMs: 1000
        maxDataPoints: 43200
        refId: "A"
      datasourceUID: "grafanacloud-prom"
      relativeTimeRange:
        from: "1h0m0s"
        to: "0s"
    B:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "A"
        reducer: "last"
        type: "reduce"
        refId: "B"
        intervalMs: 1000
        maxDataPoints: 43200
    C:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "B"
        type: "threshold"
        conditions:
          - type: "query"
            evaluator:
              type: "lt"
              params:
                - 0.9
            operator:
              type: "and"
            query:
              params:
                - "C"
            reducer:
              type: "last"
              params: []
        refId: "C"
        intervalMs: 1000
        maxDataPoints: 43200
      source: true
