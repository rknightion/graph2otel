# GENERATED by grafana/build_rules.py — do not edit by hand. Edit the RULES or
# DETECTIONS list there, then run `just rules`.
#
# Grafana App Platform AlertRule (rules.alerting.grafana.app/v0alpha1). Deploy
# with `just rules-push` (see docs/deploying-observability.md), NOT with a bare
# `gcx resources push`: `grafana.app/folder` below is a substitution token, and
# an unresolved one fails with 403 rather than filing the rule somewhere wrong.
apiVersion: "rules.alerting.grafana.app/v0alpha1"
kind: "AlertRule"
metadata:
  name: "g2o-detect-identity-risk-detection"
  annotations:
    grafana.app/folder: "REPLACE_WITH_FOLDER_UID"
    grafana.com/provenance: "api"
  labels:
    grafana.com/group: "graph2otel-detections"
    grafana.com/group-index: "7"
spec:
  title: "Entra ID Protection risk detection, medium or high"
  paused: true
  noDataState: "Ok"
  execErrState: "Error"
  trigger:
    interval: "5m"
  labels:
    severity: "critical"
    source: "entra"
    category: "identity-threat"
    pipeline: "graph2otel"
  annotations:
    summary: "Entra ID Protection raised a medium or high risk detection"
    description: "This is the detection-level evidence stream, and it answers a question the sign-in stream cannot: WHY a sign-in was risky. entra.signin carries risk_state, so a rule on it can say a session was at risk; risk_event_type exists only here and names the detection — impossible travel, an unfamiliar sign-in property, an anonymised or known-malicious address, credentials found in a public dump, a password-spray victim. Impossible travel in particular is a correlation Microsoft has already computed: expressing it as a rule over raw sign-ins would need a join across two records and a distance calculation, which Loki cannot do. The record also carries mitre_techniques, ip_address and the location fields for the investigation."
    tuning_required: "Requires Entra ID P2 — without it the endpoint answers with an empty collection and the rule can never fire, which reads exactly like a clean tenant. The event-type list is Microsoft's published set and has not been measured field-by-field here, so run the risk-detection-type hunt first: it tells you both which types your tenant actually produces and whether leakedCredentials alone already exceeds a sane page rate."
    runbook_url: "https://m7kni.io/graph2otel/runbooks/#g2o-detect-identity-risk-detection"
    __dashboardUid__: "graph2otel"
    __panelId__: "127"
    dashboard_path: "/d/graph2otel?dtab=Entra&viewPanel=127"
  expressions:
    A:
      model:
        refId: "A"
        datasource:
          type: "loki"
          uid: "grafanacloud-logs"
        expr: "sum (count_over_time({service_name=\"graph2otel\"} | event_name=`entra.risk_detection` | risk_event_type=~`(?i)(impossibletravel|mcasimpossibletravel|unfamiliarfeatures|anonymizedipaddress|maliciousipaddress|leakedcredentials|passwordspray|newcountry|suspiciousinboxmanipulation|adminconfirmedusercompromised)` | risk_level=~`(?i)(high|medium)` [5m]))"
        queryType: "range"
        editorMode: "code"
        intervalMs: 1000
        maxDataPoints: 43200
      datasourceUID: "grafanacloud-logs"
      relativeTimeRange:
        from: "1h0m0s"
        to: "0s"
    B:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "A"
        reducer: "last"
        type: "reduce"
        refId: "B"
        intervalMs: 1000
        maxDataPoints: 43200
    C:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "B"
        type: "threshold"
        conditions:
          - type: "query"
            evaluator:
              type: "gt"
              params:
                - 0
            operator:
              type: "and"
            query:
              params:
                - "C"
            reducer:
              type: "last"
              params: []
        refId: "C"
        intervalMs: 1000
        maxDataPoints: 43200
      source: true
