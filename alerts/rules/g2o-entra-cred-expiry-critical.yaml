# GENERATED by grafana/build_rules.py — do not edit by hand. Edit the RULES or
# DETECTIONS list there, then run `just rules`.
#
# Grafana App Platform AlertRule (rules.alerting.grafana.app/v0alpha1). Deploy
# with `just rules-push` (see docs/deploying-observability.md), NOT with a bare
# `gcx resources push`: `grafana.app/folder` below is a substitution token, and
# an unresolved one fails with 403 rather than filing the rule somewhere wrong.
apiVersion: "rules.alerting.grafana.app/v0alpha1"
kind: "AlertRule"
metadata:
  name: "g2o-entra-cred-expiry-critical"
  annotations:
    grafana.app/folder: "REPLACE_WITH_FOLDER_UID"
    grafana.com/provenance: "api"
  labels:
    grafana.com/group: "graph2otel-alerts"
    grafana.com/group-index: "0"
spec:
  title: "Entra app/SP credential expiring within 7 days"
  paused: false
  for: "15m0s"
  noDataState: "Ok"
  execErrState: "Error"
  trigger:
    interval: "5m"
  labels:
    severity: "critical"
    category: "credential-expiry"
    source: "entra"
    pipeline: "graph2otel"
  annotations:
    summary: "{{ $labels.owner_type }} {{ $labels.credential_type }} credential(s) expiring within 7 days (tenant {{ $labels.tenant_id }})"
    description: "entra_credentials_expiring_total is non-zero in the lt_7d/expired buckets for tenant {{ $labels.tenant_id }}, owner_type={{ $labels.owner_type }}, credential_type={{ $labels.credential_type }} for 15m. Bucket-count based, never per-credential. The runbook covers the false-positive notes and the lt_30d warning-tier companion rule."
    runbook_url: "https://m7kni.io/graph2otel/runbooks/#g2o-entra-cred-expiry-critical"
    __dashboardUid__: "graph2otel"
    __panelId__: "32"
    dashboard_path: "/d/graph2otel?dtab=Entra&viewPanel=32"
  expressions:
    A:
      model:
        datasource:
          type: "prometheus"
          uid: "grafanacloud-prom"
        editorMode: "code"
        expr: "sum by (tenant_id, owner_type, credential_type) (entra_credentials_expiring_total{expiry_bucket=~\"lt_7d|expired\"})"
        instant: false
        range: true
        intervalMs: 1000
        maxDataPoints: 43200
        refId: "A"
      datasourceUID: "grafanacloud-prom"
      relativeTimeRange:
        from: "1h0m0s"
        to: "0s"
    B:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "A"
        reducer: "last"
        type: "reduce"
        refId: "B"
        intervalMs: 1000
        maxDataPoints: 43200
    C:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "B"
        type: "threshold"
        conditions:
          - type: "query"
            evaluator:
              type: "gt"
              params:
                - 0
            operator:
              type: "and"
            query:
              params:
                - "C"
            reducer:
              type: "last"
              params: []
        refId: "C"
        intervalMs: 1000
        maxDataPoints: 43200
      source: true
