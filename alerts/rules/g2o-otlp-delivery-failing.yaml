# GENERATED by grafana/build_rules.py — do not edit by hand. Edit the RULES or
# DETECTIONS list there, then run `make rules`.
#
# Grafana App Platform AlertRule (rules.alerting.grafana.app/v0alpha1). Deploy
# with `make rules-push` (see docs/deploying-observability.md), NOT with a bare
# `gcx resources push`: `grafana.app/folder` below is a substitution token, and
# an unresolved one fails with 403 rather than filing the rule somewhere wrong.
apiVersion: "rules.alerting.grafana.app/v0alpha1"
kind: "AlertRule"
metadata:
  name: "g2o-otlp-delivery-failing"
  annotations:
    grafana.app/folder: "REPLACE_WITH_FOLDER_UID"
    grafana.com/provenance: "api"
  labels:
    grafana.com/group: "graph2otel-alerts"
    grafana.com/group-index: "18"
spec:
  title: "graph2otel OTLP export failing"
  paused: false
  noDataState: "Ok"
  execErrState: "Error"
  trigger:
    interval: "5m"
  labels:
    severity: "warning"
    category: "record-integrity"
    source: "graph2otel"
    pipeline: "graph2otel"
  annotations:
    summary: "graph2otel OTLP {{ $labels.signal }} export failing"
    description: "The exporter's own callback reported that a batch did not reach the backend in the last 15m. This is the most GENERAL data-loss signal graph2otel has: it fires on any rejection class — a size limit, a label-count limit, an expired credential, a payload cap, a limit the backend adds in future — with no requirement that anyone predicted it first. The per-limit rules (g2o-record-attrs-truncated, g2o-record-over-horizon) each guard ONE known limit; this one is the backstop behind all of them, and it is what would have caught #419 on the day it started instead of a container-log grep days later. Enabled at >0 with no baseline needed: an export failure is never a normal steady state. Grouped by signal because a metrics-side failure and a logs-side failure are different investigations. Deliberately watches export_failures ONLY — shutdown_failures moves on an ordinary restart, and a rule that fires on every deploy is a rule people learn to ignore. KNOWN BLIND SPOT, and it is why a delivery alert was originally forbidden outright (#268/#421): this rule's own evidence travels through the METRICS exporter, so it cannot be the metrics-path watchdog and its SILENCE is not proof that delivery is healthy. A total metrics outage takes the counter with it. That is what g2o-collector-staleness, /readyz and the process-local admin status are for. What this rule does cover is everything that reports itself: every logs-side failure (the metrics path is healthy throughout, which is exactly how #419 stayed queryable while being invisible), and every partial metrics-side rejection, whose accepted batches carry the counter."
    runbook_url: "https://m7kni.io/graph2otel/runbooks/#g2o-otlp-delivery-failing"
    __dashboardUid__: "graph2otel"
    __panelId__: "422"
    dashboard_path: "/d/graph2otel?dtab=Self-obs&viewPanel=422"
  expressions:
    A:
      model:
        datasource:
          type: "prometheus"
          uid: "grafanacloud-prom"
        editorMode: "code"
        expr: "sum by (signal) (increase(graph2otel_otlp_delivery_export_failures_total[15m]))"
        instant: false
        range: true
        intervalMs: 1000
        maxDataPoints: 43200
        refId: "A"
      datasourceUID: "grafanacloud-prom"
      relativeTimeRange:
        from: "1h0m0s"
        to: "0s"
    B:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "A"
        reducer: "last"
        type: "reduce"
        refId: "B"
        intervalMs: 1000
        maxDataPoints: 43200
    C:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "B"
        type: "threshold"
        conditions:
          - type: "query"
            evaluator:
              type: "gt"
              params:
                - 0
            operator:
              type: "and"
            query:
              params:
                - "C"
            reducer:
              type: "last"
              params: []
        refId: "C"
        intervalMs: 1000
        maxDataPoints: 43200
      source: true
