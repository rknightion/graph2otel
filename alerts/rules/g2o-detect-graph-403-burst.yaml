# GENERATED by grafana/build_rules.py — do not edit by hand. Edit the RULES or
# DETECTIONS list there, then run `make rules`.
#
# Grafana App Platform AlertRule (rules.alerting.grafana.app/v0alpha1). Deploy
# with `make rules-push` (see docs/deploying-observability.md), NOT with a bare
# `gcx resources push`: `grafana.app/folder` below is a substitution token, and
# an unresolved one fails with 403 rather than filing the rule somewhere wrong.
apiVersion: "rules.alerting.grafana.app/v0alpha1"
kind: "AlertRule"
metadata:
  name: "g2o-detect-graph-403-burst"
  annotations:
    grafana.app/folder: "REPLACE_WITH_FOLDER_UID"
    grafana.com/provenance: "api"
  labels:
    grafana.com/group: "graph2otel-detections"
    grafana.com/group-index: "3"
spec:
  title: "Graph API authorization-denial burst from a single application"
  paused: true
  noDataState: "Ok"
  execErrState: "Error"
  trigger:
    interval: "5m"
  labels:
    severity: "critical"
    source: "entra"
    category: "identity-threat"
    pipeline: "graph2otel"
  annotations:
    summary: "One application received more than 10 Graph 403s in 5 minutes"
    description: "A burst of authorization denials from a single caller can indicate permission probing or a compromised identity exploring what it can reach. It is equally often an application that lost a consent grant, which is worth knowing either way. Check app_id and the denied paths."
    tuning_required: "Needs entra.graph_activity, which arrives over blob ingest. The threshold of 10 in 5 minutes is a starting point from one small tenant; a tenant with more automation will need a higher one. Measure your own per-app 403 baseline before enabling."
    runbook_url: "https://m7kni.io/graph2otel/runbooks/#g2o-detect-graph-403-burst"
    __dashboardUid__: "graph2otel"
    __panelId__: "102"
    dashboard_path: "/d/graph2otel?dtab=Entra&viewPanel=102"
  expressions:
    A:
      model:
        refId: "A"
        datasource:
          type: "loki"
          uid: "grafanacloud-logs"
        expr: "sum by (app_id) (count_over_time({service_name=\"graph2otel\"} | event_name=`entra.graph_activity` | response_status_code=`403` [5m]))"
        queryType: "range"
        editorMode: "code"
        intervalMs: 1000
        maxDataPoints: 43200
      datasourceUID: "grafanacloud-logs"
      relativeTimeRange:
        from: "1h0m0s"
        to: "0s"
    B:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "A"
        reducer: "last"
        type: "reduce"
        refId: "B"
        intervalMs: 1000
        maxDataPoints: 43200
    C:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "B"
        type: "threshold"
        conditions:
          - type: "query"
            evaluator:
              type: "gt"
              params:
                - 10
            operator:
              type: "and"
            query:
              params:
                - "C"
            reducer:
              type: "last"
              params: []
        refId: "C"
        intervalMs: 1000
        maxDataPoints: 43200
      source: true
