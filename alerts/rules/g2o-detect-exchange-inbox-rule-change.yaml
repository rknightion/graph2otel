# GENERATED by grafana/build_rules.py — do not edit by hand. Edit the RULES or
# DETECTIONS list there, then run `make rules`.
#
# Grafana App Platform AlertRule (rules.alerting.grafana.app/v0alpha1). Deploy
# with `make rules-push` (see docs/deploying-observability.md), NOT with a bare
# `gcx resources push`: `grafana.app/folder` below is a substitution token, and
# an unresolved one fails with 403 rather than filing the rule somewhere wrong.
apiVersion: "rules.alerting.grafana.app/v0alpha1"
kind: "AlertRule"
metadata:
  name: "g2o-detect-exchange-inbox-rule-change"
  annotations:
    grafana.app/folder: "REPLACE_WITH_FOLDER_UID"
    grafana.com/provenance: "api"
  labels:
    grafana.com/group: "graph2otel-detections"
    grafana.com/group-index: "5"
spec:
  title: "Exchange inbox rule created or modified"
  paused: true
  noDataState: "Ok"
  execErrState: "Error"
  trigger:
    interval: "5m"
  labels:
    severity: "warning"
    source: "m365"
    category: "identity-threat"
    pipeline: "graph2otel"
  annotations:
    summary: "An Exchange inbox rule was created or changed"
    description: "A mailbox rule was created, modified or removed. Business email compromise almost always leaves one behind: a rule that moves replies from finance, or anything mentioning invoices, into a folder the owner never opens, so the victim never sees the conversation happening in their name. Microsoft classifies this as email hiding rules, MITRE T1564.008, and it is the technique the Defender BEC playbook looks for first. graph2otel carries it as an m365.audit record whose operation names the rule cmdlet; inspect user_id, client_ip and modified_property_names on the record to see who changed what."
    tuning_required: "Users create their own inbox rules constantly, so on most tenants this is the noisiest rule in the pack and needs a per-actor exclusion or a review queue rather than a pager. Run the audit-operation hunt over 30 days first: it both gives you the rate and confirms your tenant spells the operation the way this regex expects. Rule parameters are not on the record, so the rule cannot tell a forwarding rule from a housekeeping one."
    runbook_url: "https://m7kni.io/graph2otel/runbooks/#g2o-detect-exchange-inbox-rule-change"
    __dashboardUid__: "graph2otel"
    __panelId__: "350"
    dashboard_path: "/d/graph2otel?dtab=M365&viewPanel=350"
  expressions:
    A:
      model:
        refId: "A"
        datasource:
          type: "loki"
          uid: "grafanacloud-logs"
        expr: "sum (count_over_time({service_name=\"graph2otel\"} | event_name=`m365.audit` | operation=~`(?i).*inboxrule.*` [5m]))"
        queryType: "range"
        editorMode: "code"
        intervalMs: 1000
        maxDataPoints: 43200
      datasourceUID: "grafanacloud-logs"
      relativeTimeRange:
        from: "1h0m0s"
        to: "0s"
    B:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "A"
        reducer: "last"
        type: "reduce"
        refId: "B"
        intervalMs: 1000
        maxDataPoints: 43200
    C:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "B"
        type: "threshold"
        conditions:
          - type: "query"
            evaluator:
              type: "gt"
              params:
                - 0
            operator:
              type: "and"
            query:
              params:
                - "C"
            reducer:
              type: "last"
              params: []
        refId: "C"
        intervalMs: 1000
        maxDataPoints: 43200
      source: true
