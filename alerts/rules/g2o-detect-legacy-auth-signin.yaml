# GENERATED by grafana/build_rules.py — do not edit by hand. Edit the RULES or
# DETECTIONS list there, then run `just rules`.
#
# Grafana App Platform AlertRule (rules.alerting.grafana.app/v0alpha1). Deploy
# with `just rules-push` (see docs/deploying-observability.md), NOT with a bare
# `gcx resources push`: `grafana.app/folder` below is a substitution token, and
# an unresolved one fails with 403 rather than filing the rule somewhere wrong.
apiVersion: "rules.alerting.grafana.app/v0alpha1"
kind: "AlertRule"
metadata:
  name: "g2o-detect-legacy-auth-signin"
  annotations:
    grafana.app/folder: "REPLACE_WITH_FOLDER_UID"
    grafana.com/provenance: "api"
  labels:
    grafana.com/group: "graph2otel-detections"
    grafana.com/group-index: "9"
spec:
  title: "Sign-in over a legacy authentication protocol"
  paused: true
  noDataState: "Ok"
  execErrState: "Error"
  trigger:
    interval: "5m"
  labels:
    severity: "warning"
    source: "entra"
    category: "identity-threat"
    pipeline: "graph2otel"
  annotations:
    summary: "A sign-in used a legacy authentication protocol"
    description: "Legacy protocols cannot present an MFA challenge, so a sign-in that succeeds over one has bypassed multi-factor authentication no matter what the Conditional Access policy says. Password spraying targets them for exactly that reason, and Microsoft's own secure-score guidance is to block them outright. This is a protocol-level question and is deliberately separate from the risk-and-Conditional-Access sign-in detection: that rule asks whether a sign-in looked suspicious, this one asks whether a channel exists that cannot be challenged, which is worth knowing even when every sign-in on it is legitimate."
    tuning_required: "The values here are Microsoft's client-app names and have not been measured on this project's wire, so run the sign-in client-app hunt first to see which names your tenant emits and at what rate. Add status_error_code=`0` to narrow to SUCCESSFUL legacy sign-ins, which is the smaller and more urgent set; failed ones are mostly spray traffic and better handled as a trend than a page. If your tenant still has legitimate legacy clients this fires continuously and belongs on a dashboard, not a pager, until they are migrated."
    runbook_url: "https://m7kni.io/graph2otel/runbooks/#g2o-detect-legacy-auth-signin"
    __dashboardUid__: "graph2otel"
    __panelId__: "122"
    dashboard_path: "/d/graph2otel?dtab=Entra&viewPanel=122"
  expressions:
    A:
      model:
        refId: "A"
        datasource:
          type: "loki"
          uid: "grafanacloud-logs"
        expr: "sum (count_over_time({service_name=\"graph2otel\"} | event_name=`entra.signin` | client_app_used=~`(?i)(imap4?|pop3?|authenticated smtp|exchange activesync|other clients|exchange web services|exchange online powershell|autodiscover|offline address book)` [5m]))"
        queryType: "range"
        editorMode: "code"
        intervalMs: 1000
        maxDataPoints: 43200
      datasourceUID: "grafanacloud-logs"
      relativeTimeRange:
        from: "1h0m0s"
        to: "0s"
    B:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "A"
        reducer: "last"
        type: "reduce"
        refId: "B"
        intervalMs: 1000
        maxDataPoints: 43200
    C:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "B"
        type: "threshold"
        conditions:
          - type: "query"
            evaluator:
              type: "gt"
              params:
                - 0
            operator:
              type: "and"
            query:
              params:
                - "C"
            reducer:
              type: "last"
              params: []
        refId: "C"
        intervalMs: 1000
        maxDataPoints: 43200
      source: true
