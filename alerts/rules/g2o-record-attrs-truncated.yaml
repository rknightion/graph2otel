# GENERATED by grafana/build_rules.py — do not edit by hand. Edit the RULES or
# DETECTIONS list there, then run `make rules`.
#
# Grafana App Platform AlertRule (rules.alerting.grafana.app/v0alpha1). Deploy
# with `make rules-push` (see docs/deploying-observability.md), NOT with a bare
# `gcx resources push`: `grafana.app/folder` below is a substitution token, and
# an unresolved one fails with 403 rather than filing the rule somewhere wrong.
apiVersion: "rules.alerting.grafana.app/v0alpha1"
kind: "AlertRule"
metadata:
  name: "g2o-record-attrs-truncated"
  annotations:
    grafana.app/folder: "REPLACE_WITH_FOLDER_UID"
    grafana.com/provenance: "api"
  labels:
    grafana.com/group: "graph2otel-alerts"
    grafana.com/group-index: "16"
spec:
  title: "graph2otel clipped a record's attributes to fit the backend size limit"
  paused: false
  noDataState: "Ok"
  execErrState: "Error"
  trigger:
    interval: "5m"
  labels:
    severity: "warning"
    category: "record-integrity"
    source: "graph2otel"
    pipeline: "graph2otel"
  annotations:
    summary: "Collector {{ $labels.collector }} emitted an oversized record on {{ $labels.ingest_transport }} (tenant {{ $labels.tenant_id }})"
    description: "At least one log record exceeded the backend's structured-metadata size limit in the last 15m and had its largest attribute values shortened to fit (#419). This is CONTENT loss, not record loss: the record landed, but one or more fields are truncated. Default-enabled at >0 for two reasons — the measured rate is 2-3 records per day, so it is not noisy, and the clipped record's attrs_truncated_keys is currently the ONLY way to identify which record shape is oversized, since #419's live-wire sweep cleared every reachable source. Treat the first firing as the diagnostic it is."
    runbook_url: "https://m7kni.io/graph2otel/runbooks/#g2o-record-attrs-truncated"
    __dashboardUid__: "graph2otel"
    __panelId__: "485"
    dashboard_path: "/d/graph2otel?dtab=Self-obs&viewPanel=485"
  expressions:
    A:
      model:
        datasource:
          type: "prometheus"
          uid: "grafanacloud-prom"
        editorMode: "code"
        expr: "sum by (tenant_id, collector, ingest_transport) (increase(graph2otel_event_attrs_truncated_total[15m]))"
        instant: false
        range: true
        intervalMs: 1000
        maxDataPoints: 43200
        refId: "A"
      datasourceUID: "grafanacloud-prom"
      relativeTimeRange:
        from: "1h0m0s"
        to: "0s"
    B:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "A"
        reducer: "last"
        type: "reduce"
        refId: "B"
        intervalMs: 1000
        maxDataPoints: 43200
    C:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "B"
        type: "threshold"
        conditions:
          - type: "query"
            evaluator:
              type: "gt"
              params:
                - 0
            operator:
              type: "and"
            query:
              params:
                - "C"
            reducer:
              type: "last"
              params: []
        refId: "C"
        intervalMs: 1000
        maxDataPoints: 43200
      source: true
