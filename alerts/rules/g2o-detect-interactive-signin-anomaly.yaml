# GENERATED by grafana/build_rules.py — do not edit by hand. Edit the RULES or
# DETECTIONS list there, then run `make rules`.
#
# Grafana App Platform AlertRule (rules.alerting.grafana.app/v0alpha1). Deploy
# with `make rules-push` (see docs/deploying-observability.md), NOT with a bare
# `gcx resources push`: `grafana.app/folder` below is a substitution token, and
# an unresolved one fails with 403 rather than filing the rule somewhere wrong.
apiVersion: "rules.alerting.grafana.app/v0alpha1"
kind: "AlertRule"
metadata:
  name: "g2o-detect-interactive-signin-anomaly"
  annotations:
    grafana.app/folder: "REPLACE_WITH_FOLDER_UID"
    grafana.com/provenance: "api"
  labels:
    grafana.com/group: "graph2otel-detections"
    grafana.com/group-index: "4"
spec:
  title: "Interactive sign-in blocked by Conditional Access or flagged at risk"
  paused: true
  noDataState: "Ok"
  execErrState: "Error"
  trigger:
    interval: "5m"
  labels:
    severity: "critical"
    source: "entra"
    category: "identity-threat"
    pipeline: "graph2otel"
  annotations:
    summary: "An interactive sign-in was CA-blocked or risk-flagged"
    description: "A real user sign-in that Conditional Access blocked, or that Entra ID Protection scored atRisk or confirmedCompromised. Check user_principal_name, app_display_name and ip_address on the record. The tenant this came from adds a third clause for sign-ins outside its expected country; that is a per-tenant policy statement rather than a portable default, so it is not shipped. Add another OR term filtering location_country_or_region against your own country code if you want it."
    tuning_required: "Conditional Access failures include ordinary events such as a user declining an MFA prompt, so on most tenants this needs either a threshold above zero or a narrowing to risk_state alone. Measure your CA failure rate over 30 days first. Risk states require Entra ID P2."
    runbook_url: "https://m7kni.io/graph2otel/runbooks/#g2o-detect-interactive-signin-anomaly"
    __dashboardUid__: "graph2otel"
    __panelId__: "86"
    dashboard_path: "/d/graph2otel?dtab=Entra&viewPanel=86"
  expressions:
    A:
      model:
        refId: "A"
        datasource:
          type: "loki"
          uid: "grafanacloud-logs"
        expr: "(sum (count_over_time({service_name=\"graph2otel\"} | event_name=`entra.signin` | sign_in_event_types=`interactiveUser` | conditional_access_status=`failure` [5m]))) + (sum (count_over_time({service_name=\"graph2otel\"} | event_name=`entra.signin` | sign_in_event_types=`interactiveUser` | risk_state=~`atRisk|confirmedCompromised` [5m])))"
        queryType: "range"
        editorMode: "code"
        intervalMs: 1000
        maxDataPoints: 43200
      datasourceUID: "grafanacloud-logs"
      relativeTimeRange:
        from: "1h0m0s"
        to: "0s"
    B:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "A"
        reducer: "last"
        type: "reduce"
        refId: "B"
        intervalMs: 1000
        maxDataPoints: 43200
    C:
      model:
        datasource:
          type: "__expr__"
          uid: "__expr__"
        expression: "B"
        type: "threshold"
        conditions:
          - type: "query"
            evaluator:
              type: "gt"
              params:
                - 0
            operator:
              type: "and"
            query:
              params:
                - "C"
            reducer:
              type: "last"
              params: []
        refId: "C"
        intervalMs: 1000
        maxDataPoints: 43200
      source: true
